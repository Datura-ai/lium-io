[project]
name = "datura"
version = "0.1.2"
description = "Compute subnet shared library"
authors = [
    {name = "waris", email = "waris0609@outlook.com"},
]
dependencies = []
requires-python = "<3.13,>=3.11"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[dependency-groups]
dev = ["pytest>=8.4.2", "pytest-mock>=3.15.0"]

[tool.pdm]
distribution = true
