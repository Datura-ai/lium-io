---
# Localhost-only by design: a provider runs this on the host it is provisioning.
# There is no remote transport and no inventory of other machines.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
