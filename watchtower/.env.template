# Watchtower Configuration
# Automatically monitors and updates Docker images with validator-signed digests

# Enable/disable watchtower service
WATCHTOWER_ENABLED=true

# Docker image to monitor (full image name including registry)
WATCHTOWER_IMAGE=daturaai/compute-subnet-executor-runner

# Check interval in seconds (default: 300 = 5 minutes)
WATCHTOWER_INTERVAL=300

# The digest endpoint and the validator hotkeys (WATCHTOWER_ENDPOINT_URL, WATCHTOWER_VALIDATOR_HOTKEY,
# WATCHTOWER_VALIDATOR_HOTKEY_NEXT) are not read from this file: they are constants in src/config.py,
# overridden at build time by docker_build.sh (README, "Build & Deploy").
